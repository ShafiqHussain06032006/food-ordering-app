
  # Update Color Scheme (Copy)

  This is a code bundle for Update Color Scheme (Copy). The original project is available at https://www.figma.com/design/QDA4HSmCOp6qaQqVjQQ355/Update-Color-Scheme--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  